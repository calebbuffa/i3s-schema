{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "geometryDefinition",
  "description": "The geometry definitions used in [Point Scene Layer]() I3S version 1.7.",
  "type": "object",
  "properties": {
    "topology": {
      "type": "string",
      "enum": [
        "point"
      ],
      "description": "Defines the topology type of the point. Must be: `point`"
    },
    "geometryBuffers": {
      "type": "array",
      "items": {
        "$ref": "geometryBuffer.schema.json"
      },
      "description": "Array of geometry representation(s) for this class of points. Must be compressed."
    }
  },
  "required": [
    "geometryBuffers"
  ],
  "x-i3s-versions": [
    "1.10",
    "1.9",
    "1.8"
  ]
}
