{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "elevationInfo",
  "description": "The elevationInfo defines how content in a scene layer is aligned to the ground.",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "description": "The mode of the elevation. Point cloud scene layer supports absoluteHeight."
    },
    "offset": {
      "type": "number",
      "description": "The offset the point cloud scene layer. The elevation unit is the coordinate systems units."
    }
  },
  "required": [
    "mode"
  ],
  "x-i3s-versions": [
    "2.1",
    "2.0"
  ]
}
