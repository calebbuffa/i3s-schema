{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "meshMaterial",
  "description": "Mesh geometry for a node.",
  "type": "object",
  "properties": {
    "definition": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4294967295,
      "description": "The index in [layer.materialDefinitions](3DSceneLayer.cmn.md) array."
    },
    "resource": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4294967295,
      "description": "Resource id for the material textures. i.e: `layers/0/nodes/{material.resource}/textures/{tex_name}`. Is **required** if material declares any textures."
    },
    "texelCountHint": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4294967295,
      "description": "Estimated number of texel for the highest resolution base color texture. i.e. `texture.mip0.width*texture.mip0.height`. Useful to estimate the resource cost of this node and/or texel-resolution based LOD switching. Ignored for un-textured meshes."
    }
  },
  "required": [
    "definition"
  ],
  "x-i3s-versions": [
    "1.10",
    "1.9",
    "1.8",
    "1.7"
  ]
}
