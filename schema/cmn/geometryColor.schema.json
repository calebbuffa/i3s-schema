{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "geometryColor",
  "description": "The color vertex attribute. Assumed to be Standard RGB (sRGB space). sRGB is a color space that defines a range of colors that can be displayed on screen on in print. It is the most widely used color space and is supported by most operating systems, software programs, monitors, and printers.",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "UInt8"
      ],
      "description": "The color channel values. Must be: `UInt8`"
    },
    "component": {
      "type": "integer",
      "description": "Number of colors. Must be `1` (opaque grayscale: `{R,R,R,255}`),`3`(opaque color `{R,G,B,255}`) or `4` ( transparent color `{R,G,B,A}`)."
    },
    "encoding": {
      "type": "string",
      "enum": [
        "normalized"
      ],
      "description": "Encoding of the vertex attribute. Must be: `normalized`: Default. Assumes 8-bit unsigned color per channel [0,255] -> [0,1]."
    },
    "binding": {
      "type": "string",
      "enum": [
        "per-vertex"
      ],
      "description": "Must be: `per-vertex`"
    }
  },
  "required": [
    "type",
    "component"
  ],
  "x-i3s-versions": [
    "1.10",
    "1.9",
    "1.8",
    "1.7"
  ]
}
