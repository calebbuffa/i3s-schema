{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "resource",
  "description": "Resource objects are pointers to different types of resources related to a node, such as the feature data, the geometry attributes and indices, textures and shared resources.",
  "type": "object",
  "properties": {
    "href": {
      "type": "string",
      "description": "The relative URL to the referenced resource."
    },
    "layerContent": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "**Deprecated.** The list of layer names that indicates which layer features in the bundle belongs to. The client can use this information to selectively download bundles.",
      "deprecated": true
    },
    "featureRange": {
      "type": "array",
      "items": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295
      },
      "description": "**Deprecated.** Only applicable for featureData resources. Provides inclusive indices of the features list in this node that indicate which features of the node are located in this bundle.",
      "deprecated": true
    },
    "multiTextureBundle": {
      "type": "string",
      "description": "**Deprecated.** Only applicable for textureData resources. TRUE if the bundle contains multiple textures. If FALSE or not set, clients can interpret the entire bundle as a single image.",
      "deprecated": true
    },
    "vertexElements": {
      "type": "array",
      "items": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295
      },
      "description": "**Deprecated.** Only applicable for geometryData resources. Represents the count of elements in vertexAttributes; multiply by the sum of bytes required for each element as defined in the defaultGeometrySchema.",
      "deprecated": true
    },
    "faceElements": {
      "type": "array",
      "items": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295
      },
      "description": "**Deprecated.** Only applicable for geometryData resources. Represents the count of elements in faceAttributes; multiply by the sum of bytes required for each element as defined in the defaultGeometrySchema.",
      "deprecated": true
    }
  },
  "required": [
    "href"
  ],
  "x-i3s-versions": [
    "1.10",
    "1.9",
    "1.8",
    "1.7",
    "1.6"
  ]
}
