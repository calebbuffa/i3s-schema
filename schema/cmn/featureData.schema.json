{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "featureData",
  "description": "The FeatureData JSON file(s) contain geographical features with a set of attributes, accessors to geometry attributes, and other references to styling or materials. FeatureData is only used by point scene layers. For other scene layer types, such as 3D object scene layer or integrated mesh scene layer, clients read [defaultGeometrySchema](defaultGeometrySchema.cmn.md) to access the geometry buffer.",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4294967295,
      "description": "Feature ID, unique within the Node. If lodType is FeatureTree, the ID must be unique in the store."
    },
    "position": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "An array of two or three doubles, giving the x,y(,z) (easting/northing/elevation) position of this feature's minimum bounding sphere center, in the vertexCRS."
    },
    "pivotOffset": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 3,
      "maxItems": 3,
      "description": "An array of three doubles, providing an optional, 'semantic' pivot offset that can be used to e.g. correctly drape tree symbols."
    },
    "mbb": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 6,
      "maxItems": 6,
      "description": "An array of six doubles, corresponding to xmin, ymin, zmin, xmax, ymax and zmax of the minimum bounding box of the feature, expressed in the vertexCRS, without offset. The mbb can be used with the Feature’s Transform to provide a LOD0 representation without loading the GeometryAttributes."
    },
    "layer": {
      "type": "string",
      "description": "The name of the Feature Class this feature belongs to."
    },
    "attributes": {
      "$ref": "featureAttribute.schema.json",
      "description": "The list of GIS attributes the feature has."
    },
    "geometries": {
      "$ref": "geometry.schema.json",
      "description": "The list of geometries the feature has. A feature always has at least one Geometry."
    }
  },
  "required": [
    "id"
  ],
  "x-i3s-versions": [
    "1.10",
    "1.9",
    "1.8",
    "1.7",
    "1.6"
  ]
}
