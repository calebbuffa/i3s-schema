{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "image",
  "description": "An image is a binary resource, containing a single raster that can be used to texture a feature or symbol. An image represents one specific texture LoD. For details on texture organization, please refer to the section on [texture resources](texture.cmn.md).",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "A unique ID for each image. Generated using the BuildID function."
    },
    "size": {
      "type": "integer",
      "minimum": 0,
      "maximum": 4294967295,
      "description": "width of this image, in pixels."
    },
    "pixelInWorldUnits": {
      "type": "number",
      "description": "The maximum size of a single pixel in world units. This property is used by the client to pick the image to load and render."
    },
    "href": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "The href to the image(s), one per encoding, in the same order as the encodings."
    },
    "byteOffset": {
      "type": "array",
      "items": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295
      },
      "description": "The byte offset of this image's encodings. There is one per encoding, in the same order as the encodings, in the block in which this texture image resides."
    },
    "length": {
      "type": "array",
      "items": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295
      },
      "description": "The length in bytes of this image's encodings. There is one per encoding, in the same order as the encodings."
    }
  },
  "required": [
    "id"
  ],
  "x-i3s-versions": [
    "1.10",
    "1.9",
    "1.8",
    "1.7",
    "1.6"
  ]
}
