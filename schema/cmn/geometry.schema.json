{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "geometry",
  "description": "This is the common container class for all types of geometry definitions used in I3S.",
  "type": "object",
  "properties": {
    "id": {
      "type": "number",
      "description": "Unique ID of the geometry in this store."
    },
    "type": {
      "type": "string",
      "description": "The type denotes whether the following geometry is defined by using array buffer views (ArrayBufferView), as an internal reference (GeometryReference), as a reference to a shared Resource (SharedResourceReference) or embedded (Embedded)."
    },
    "transformation": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 16,
      "maxItems": 16,
      "description": "3D (4x4) transformation matrix expressed as a linear array of 16 values. Used for methods such as translation, scaling, and rotation."
    },
    "params": {
      "$ref": "geometryParams.schema.json",
      "description": "The parameters for a geometry, as an Embedded GeometryParams object, an ArrayBufferView, a GeometryReference object, or a SharedResourceReference object."
    }
  },
  "required": [
    "id"
  ],
  "x-i3s-versions": [
    "1.10",
    "1.9",
    "1.8",
    "1.7",
    "1.6"
  ]
}
