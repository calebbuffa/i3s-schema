{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "textureSetDefinition",
  "type": "object",
  "properties": {
    "formats": {
      "type": "array",
      "items": {
        "$ref": "textureSetDefinitionFormat.schema.json"
      },
      "description": "List of formats that are available for this texture set."
    },
    "atlas": {
      "type": "boolean",
      "description": "Set to `true` if this texture is a texture atlas. It is expected that geometries that use this texture have uv regions to specify the subtexture in the atlas."
    }
  },
  "required": [
    "formats"
  ],
  "x-i3s-versions": [
    "1.10",
    "1.9",
    "1.8",
    "1.7"
  ]
}
